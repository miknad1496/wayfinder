node_modules/
.env
backend/data/sessions/
backend/data/feedback/
backend/data/users/
backend/data/learned/
backend/data/backup/
backend/data/invites/
backend/data/anon-rate-limits.json
frontend/dist/
seed-data/
*.log
backend/logs/
.DS_Store
Thumbs.db
package-lock.json

# Sensitive / local-only folders
APIs Tokens/
Pathivy emails/
research/

# Scraped data: ignore raw intermediate files, but TRACK the curated JSON datasets
# These JSON files ARE the knowledge database and must deploy with the app
# backend/data/scraped/*.json — TRACKED (committed to repo)
# backend/data/scraped/*.md — TRACKED (markdown summaries)
# backend/data/scraped/*.csv — ignored (bulk intermediates)
