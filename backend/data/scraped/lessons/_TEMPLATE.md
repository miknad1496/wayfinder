# {Task ID} — Lessons Learned

> This file accumulates takeaways across runs. Each run reads it at start
> (to inherit prior context) and appends to it at end (to teach future runs).
> Sections may be added, refined, or condensed by the LLM as patterns emerge.

## CURRENT CALIBRATION (latest accepted values)

- batch_size: [N]
- typical_run_duration: [minutes]
- typical_skip_rate: [%]
- last_calibration_change: [date — what changed and why]

## EFFECTIVE PATTERNS (use these — they work)

(empty on bootstrap; populated as runs discover what works)

## FAILED PATTERNS / KNOWN ANTI-PATTERNS (don't repeat)

(empty on bootstrap; populated as runs discover what doesn't)

## SOURCE-SPECIFIC NOTES

### URLs / domains that fail
(e.g. "bhs.bisd303.org → 600KB+ pages, WebFetch always fails, go straight to site:bisd303.org WebSearch")

### High-yield workarounds
(e.g. "For principal names: site:[district].org '[school name]' principal")

## DATA QUALITY FLAGS DISCOVERED

(real-world things found while running — defunct programs, mis-classified entries, schema inconsistencies — recorded with date)

## CALIBRATION SUGGESTIONS FROM PAST RUNS

(latest run's recommendation for the next run — batch size, focus area, timing, etc.)

## OPEN QUESTIONS / TODO FOR FUTURE RUNS

(things the LLM noticed but couldn't address in one run)

## RUN HISTORY (most recent 10 runs — older entries get summarized + pruned)

| Date | Run # | Verified | Skipped | Duration | Notable Issue |
|------|-------|----------|---------|----------|---------------|
| ...  | ...   | ...      | ...     | ...      | ...           |
